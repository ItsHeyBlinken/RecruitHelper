# PostgreSQL connection (used by backend and scraper)
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/recruitconnect

# Backend server (3001 locally; production defaults to 80 for Coolify)
PORT=3001
NODE_ENV=development

# Frontend (Vite dev server proxies to backend)
VITE_API_URL=http://localhost:3001
